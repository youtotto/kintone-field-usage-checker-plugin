# フィールド使用状況チェッカー（Field Usage Checker）

## 公式リンク

- [無料版の配布ページ](https://github.com/youtotto/kintone-field-usage-checker-plugin)
- [紹介ページ](https://www.nestrec.com/post/kintone-free-plugins-renewal)

## ダウンロードと導入

[Releases](https://github.com/youtotto/kintone-field-usage-checker-plugin/releases/latest) から次のファイルをダウンロードします。

| ファイル | 用途 |
|---|---|
| `field-usage-checker-1.2.0-free-bundle.zip` | 説明書付きの配布用 ZIP（おすすめ）。**ZIP を解凍し、中の `field-usage-checker-1.2.0-free-plugin.zip` を kintone へ読み込みます。bundle 自体は kintone に直接読み込みません** |
| `field-usage-checker-1.2.0-free-plugin.zip` | kintone に直接読み込むプラグイン（bundle の中身と同じファイル） |
| `SHA256SUMS.txt` | 上記 ZIP の SHA-256 |

kintone への読み込みは、kintone システム管理 →「プラグイン」→「読み込む」で `field-usage-checker-1.2.0-free-plugin.zip` を ZIP のまま選びます。

「このフィールド、どこで使っていたっけ？」を確認するための無料 kintone プラグインです。

アプリのフィールド一覧と、そのアプリの設定（計算式・ルックアップ・関連レコード一覧・一覧・通知・プロセス管理・グラフ・アプリアクション・アクセス権）の中で各フィールドがどこで使われているかを、プラグイン設定画面に一覧表示します。レコードのデータには一切触れない、読み取り専用のツールです。

- 🆓 無料・MIT License
- 🔍 1 アプリ内の kintone 設定から確認できる使用箇所を一覧
- 📝 フィールドごとの開発メモ
- 🏷 レコード詳細画面にフィールドコードを表示（オプション）

---

## 主な機能

### フィールド一覧
- フォームのレイアウト順に、フィールド名称・フィールドコード・フィールドタイプ・必須・重複禁止・初期値を一覧表示
- サブテーブル内のフィールド、関連レコード一覧の表示フィールドは子行（┗）として表示
- レイアウトに置かれていないシステムフィールド（ステータス・作業者など）も表示

### 使用箇所の検出
フォーム内の全フィールドコード（サブテーブル内を含む）を起点に、次の設定から使用箇所を検出し、種類と対象名のバッジで表示します（例: `一覧: 顧客一覧`、`通知: 契約期限通知`、`グラフ: 月別売上`）。

| 種類 | 検出する設定 |
|---|---|
| 計算式 | 計算フィールドの計算式、文字列（1 行）の自動計算 |
| ルックアップ | ルックアップフィールド自身、「ほかのフィールドのコピー」のコピー先 |
| 関連レコード | 関連レコード一覧の「表示対象のレコードの条件」に使うこのアプリ側のフィールド |
| 一覧 | 表示する項目、絞り込み条件、並び替え、カレンダー形式の日付・タイトル |
| 通知 | アプリの条件通知の通知先、レコードの条件通知の条件式と通知先 |
| リマインダー | リマインダーの条件通知の基準日時・条件式・通知先 |
| プロセス管理 | アクションの実行条件、ステータスの作業者に指定したフィールド |
| グラフ | 分類する項目、集計方法の対象、並び替え、絞り込み条件 |
| アプリアクション | コピー元のフィールド、実行条件 |
| アクセス権 | レコードのアクセス権の条件式・対象フィールド、フィールドのアクセス権の対象フィールド |

条件式（絞り込み条件・並び替え・計算式）は、フィールドコードを識別子の境界で照合します。`date` と `update_date`、`A` と `ABC` を区別し、文字列リテラル（`"date"`）の中の偶然の一致は参照として扱いません。

### 検索・フィルター・件数
- フィールドコード・名称による絞り込み
- 「すべて / 使用箇所あり / 未検出」のフィルター
- 判定対象のフィールド数、使用箇所あり・未検出の件数、表示中の件数

### メモ
- フィールドごとにメモを残せます（プラグイン設定に保存。レコードには保存しません）

### レコード詳細画面のフィールドコード表示
- 設定でオンにすると、レコード詳細画面のフィールドラベルの横に `[フィールドコード]` を表示します
- サブテーブル内のフィールド、グループ、関連レコード一覧など、API で要素を取得できないものは対象外です

---

## 「未検出」について

**「未検出」は、そのフィールドが削除して安全であることを保証するものではありません。**

本ツールは、現在のアプリ設定から確認できる利用箇所だけを表示します。次のものは対象外で、これらで使われているフィールドも「未検出」と表示されます。

- JavaScript / CSS カスタマイズ（`record.フィールドコード.value` など）
- REST API 経由の参照（外部システム・連携サービス・他のプラグインからのアクセス）
- 他アプリからの参照（他アプリのルックアップの参照先・コピー元、他アプリの関連レコード一覧の参照先）
- カスタムビューの HTML
- プラグインの設定

また、設定の取得に失敗した項目（権限がない場合など）は判定に含まれず、画面上部に一覧表示されます。フィールドを削除・変更する前に、これらの対象外の利用箇所も必ず確認してください。

### AppInsight for kintone
JavaScript カスタマイズの解析や他アプリからの被参照を含む依存関係の分析は、[AppInsight for kintone](https://www.nestrec.com/appinsight) で確認できます。本プラグインは 1 アプリを手早く確認する用途に絞っています。

---

## 使い方

1. kintone の「プラグイン」管理画面から本プラグインを読み込み、対象アプリに追加します
2. アプリの設定 → プラグイン → 本プラグインの「設定」を開くと、フィールド一覧と使用箇所が表示されます
3. 必要に応じてメモを入力し、「保存」を押します（メモと「フィールドコードを表示する」設定が保存されます）
4. 「レコード詳細画面のフィールドラベルの横にフィールドコードを表示する」をオンにして保存し、アプリを更新すると、詳細画面にフィールドコードが表示されます

設定画面を開く人には、アプリの管理権限が必要です。アクセス権（レコード・フィールド）などの設定を取得できない場合は、その旨が画面に表示され、取得できた設定だけで判定します。

---

## 対応環境

- kintone PC 版（レコード詳細画面、プラグイン設定画面）
- ゲストスペースのアプリに対応
- モバイル版には対応していません

---

## 更新履歴

### v1.2.0
- 無料公開版。ライセンス確認機能と試用版を廃止
- 判定の起点をフォーム内の全フィールドコードに変更（一覧に表示されていないフィールドも条件式などから検出）
- 検出対象を拡充: 計算式、ルックアップ、関連レコード一覧の条件、一覧の絞り込み・並び替え・カレンダー、アプリの条件通知・レコードの条件通知・リマインダーの通知先、プロセス管理の作業者、アクセス権
- 条件式の照合を識別子境界・文字列リテラル考慮に変更（部分一致による誤検出を解消）
- 「未検出」表示と常設の注意書き。JavaScript の簡易検出を廃止し対象外として明記
- 使用箇所を種類と対象名のバッジで表示。検索・フィルター・件数を追加
- 関連レコード一覧の子行のメモを複合キーで保存（他アプリのフィールドコードとの衝突を解消）
- 設定取得の失敗・関連先アプリの閲覧不可・設定データの破損でも画面が固まらないように変更
- ゲストスペースに対応
- 詳細画面のフィールドコード表示を `getFieldElement` 基準に変更（同名ラベルでの誤表示を解消）

---

## ライセンス

MIT License（[LICENSE](./LICENSE)）。商用利用・改変・再配布が可能です。

同梱の `css/51-modern-default.css` は Cybozu, Inc. が MIT License で公開しているプラグイン用スタイルシートです（Copyright (c) 2014 Cybozu）。

---

## 作者

NestRec https://github.com/youtotto
kintone プラグイン／業務改善ツール開発
